// Empty stub to satisfy Vite's static import analysis of glb-pack/web's
// `import("canvas")` Node-only fallback. In a real browser, glb-pack/web's
// `hasNativeCanvas()` returns true and the native Canvas2D path runs FIRST,
// so the dynamic import is never executed at runtime — this stub just keeps
// the bundler from erroring on a missing module specifier.
export {};
